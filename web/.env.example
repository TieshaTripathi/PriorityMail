# Public values only. Vite embeds VITE_* values in the browser bundle.
VITE_API_BASE_URL=
# Future Google OAuth public client ID (no client secret here).
VITE_GOOGLE_CLIENT_ID=
# Future Web Push public VAPID key. Keep private key on the server.
VITE_VAPID_PUBLIC_KEY=
