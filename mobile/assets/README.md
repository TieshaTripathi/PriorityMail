# PriorityMail artwork to supply

No binary placeholders have been generated. Add these PNGs here:

- `icon.png`: 1024 × 1024, square, opaque background, no pre-rounded corners.
- `adaptive-icon.png`: 1024 × 1024 transparent foreground; keep the mark inside the central 66% safe area. Android background is white.
- `splash.png`: 1024 × 1024 transparent centered logo, displayed at 200 points on white.

`app.config.js` automatically enables each image when present. Until then Expo defaults are used, with a white splash screen. Supply artwork before a branded preview/store build; rebuild to change the home-screen icon. The app display name is already PriorityMail.
